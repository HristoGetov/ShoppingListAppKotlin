<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.ShoppingListaAppKotlin"
        tools:targetApi="31">
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.App.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.FriendsListActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity" />
        <activity
            android:name=".presentation.viewModel.FindFriendsActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity" />
        <activity
            android:name=".presentation.SignUpActivity"
            android:exported="false"/>
        <activity
            android:name=".presentation.SignInActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".presentation.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ShoppingListaAppKotlin" />
    </application>

</manifest>