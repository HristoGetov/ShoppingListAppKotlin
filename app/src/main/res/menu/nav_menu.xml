<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:id="@+id/send_list"
            android:title="@string/send_list"/>

        <item android:id="@+id/friends_list"
            android:title="@string/friends_list"/>
        <item
            android:id="@+id/find_people"
            android:title="@string/find_friends"/>
    </group>

    <item android:title="@string/Settings">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/system_settings"
                    android:title="@string/system_settings"/>
                <item android:id="@+id/logout"
                    android:title="@string/log_out"/>
            </group>
        </menu>
    </item>
</menu>